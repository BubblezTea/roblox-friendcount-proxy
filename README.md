# Roblox Friend Count Proxy

This is a Node.js Express proxy server that fetches the friend count of a Roblox user using Roblox's public API.

## Endpoint

**GET** `/friendcount?userid=USER_ID`

### Example:
